# 💻 Developer — LP 프로토타입 코드를 QA(Quality Assurance) 환경에 배포하고, 트래킹 로직의 A/B 테스트 변수 작동 여부와 모든 CTA 버튼 클릭 시 'Gap 인지 시간 ($	ext{Time}_{	ext{Gap}}$)' 타이머가 정확하게 측정되는지 검증하는 테스트 보고서 초안을 작성하십시오. (버그 리포트 및 최종 배포 전 체크리스트 포함)

💻 Developer: 작업 시작합니다.

# 📄 LP 프로토타입 QA 테스트 보고서 초안 (v1.0)
**문서 목적:** 리드 마그넷 다운로드 페이지(MVP)의 기능적 안정성, 트래킹 로직(특히 $\text{Time}_{\text{Gap}}$ 측정), 그리고 A/B 테스트 변수의 작동 여부를 검증합니다. 본 보고서는 최종 배포 전 반드시 확인되어야 할 핵심 버그 및 체크리스트를 포함합니다.

---

## 🛠️ 1. 테스트 환경 및 사전 조건

*   **테스트 URL:** `[Staging Environment] /lp/blueprint-mvp`
*   **필요 API 키:** `API_TRACKING_ID`, `CRM_WEBHOOK_KEY` (환경변수 사용)
*   **핵심 기능 검증 항목:**
    1.  사용자 여정(UX Flow): 스크롤링 및 섹션 간 논리적 전환의 매끄러움.
    2.  트래킹 로직: 모든 CTA 클릭 시 `ga` 또는 `beacon`을 통해 이벤트가 전송되는지 확인.
    3.  시간 측정: CTA 클릭 직후 $\text{Time}_{\text{Gap}}$ 타이머 시작 여부 및 정확성 검증.

## 🧪 2. 핵심 기능별 테스트 케이스 (Test Cases)

| ID | 영역 | 시나리오 | 기대 결과 (Expected Result) | 실제 결과 (Actual Result) | 상태 | 비고 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01** | 기본 흐름 | 랜딩 페이지 진입 및 스크롤링 | 모든 섹션이 정상적으로 로드되며, 논리적 경고 문구(Misconception Alert)가 강조되어 노출됨. | (실제 테스트 필요) | Pass/Fail | |
| **TC-02** | $\text{Time}_{\text{Gap}}$ 측정 | 1차 CTA (`Mini Checklist 다운로드`) 클릭 시 | ① `cta_click` 이벤트 발생. ② 타이머 카운트다운이 즉시 시작되며, 백엔드 로그에 정확한 `timestamp`가 기록됨. | (실제 테스트 필요) | Pass/Fail | **최우선 검증 항목** |
| **TC-03** | $\text{Time}_{\text{Gap}}$ 측정 | 2차 CTA (`Worksheet 구매`) 클릭 시 | ① `cta_click` 이벤트 발생. ② 타이머 카운트다운이 즉시 시작되며, 백엔드 로그에 정확한 `timestamp`가 기록됨. 결제 모달(Mockup) 활성화. | (실제 테스트 필요) | Pass/Fail | **최우선 검증 항목** |
| **TC-04** | A/B 테스트 변수 | '긴급성 타이머' 노출 그룹 (A안) | 현재 시간 대비 남은 가상 시간을 보여주는 카운트다운이 1초 단위로 정확하게 감소하며, 시각적 불안감을 조성함. | (실제 테스트 필요) | Pass/Fail | |
| **TC-05** | A/B 테스트 변수 | '잠재적 비용 문구' 노출 그룹 (B안) | 잠재적인 손실(Loss)로 인한 재정적 위험을 경고하는 구체적 문구가 적절한 섹션에 배치되며, 스크롤 시 함께 움직임. | (실제 테스트 필요) | Pass/Fail | |
| **TC-06** | 최종 전환 로직 | 3차 CTA (`Blueprint 구매`) 클릭 및 제출 | ① `cta_click` 이벤트 발생. ② 타이머 시작. ③ 데이터 수집 폼(이메일, 직군 등)의 유효성 검사 후 정상적으로 CRM 웹훅 전송 확인. ④ 성공 페이지로 리디렉션됨. | (실제 테스트 필요) | Pass/Fail | **최우선 검증 항목** |

## 🐞 3. 버그 리포트 (Bug Report - 발견된 문제점 기록)

*   *(주: 이 섹션은 현재 프로토타입 코드의 잠재적 결함을 가정한 보고서입니다.)*

| Bug ID | 심각도 | 발생 위치 | 재현 단계 (Steps to Reproduce) | 예상 결과 (Expected Result) | 실제 결과 (Actual Result) | 담당 에이전트 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-001** | Critical | TC-02, TC-03 | 1. LP 진입 $\to$ 스크롤링하여 Mini Checklist 섹션으로 이동. 2. CTA 버튼 클릭 시도. | `ga` 이벤트가 트리거되고 타이머 카운트다운이 시작되어야 함. | `cta_click` 이벤트는 발생했으나, JavaScript 오류로 인해 **타이머 로직(Countdown Timer)**이 아예 작동하지 않거나 1초 지연됨. 콘솔에 `TypeError: timerElement is null` 에러 메시지 출력. | Developer |
| **BUG-002** | Major | TC-06 | 최종 구매 버튼 클릭 후, 필수 정보 필드(직군)를 비워두고 제출 시도. | 클라이언트 측 유효성 검사(Client-side validation)가 작동하며 빨간색 경고 문구와 함께 폼이 무효화되어야 함. | 경고 문구가 보이지 않으며, 백그라운드에서 **빈 데이터**로 웹훅 요청을 보내는 것으로 추정됨. (API 측 로직 누락 의심). | Developer/Business |
| **BUG-003** | Minor | 전반적 | 페이지를 새로고침(Hard Refresh) 할 경우. | `localStorage`에 저장된 사용자의 스크롤 위치나 A/B 테스트 그룹 정보가 유지되어야 함. | 모든 상태 데이터(State Data)가 초기화됨. 사용자가 처음부터 다시 시작하는 느낌을 받음. | Developer |

## ✅ 4. 최종 배포 전 체크리스트 (Pre-Deployment Checklist)

| 구분 | 항목 | 담당자 | 완료 여부 | 비고 (Action Items) |
| :--- | :--- | :--- | :--- | :--- |
| **[개발] 기술 검증** | BUG-001, 002 수정 및 테스트 완료. | Developer | $\square$ | 특히 타이머 시작 시점의 `try...catch` 에러 핸들링 강화 필요. |
| **[개발] 트래킹 로직** | 모든 CTA에 대한 이벤트 리스너가 완벽히 작동하는지 최종 확인 (GA/GTM). | Developer | $\square$ | 백엔드 로그(S3 버킷 등)에서 `timestamp` 데이터의 무결성을 1시간 동안 모니터링해야 함. |
| **[디자인] UX 검증** | 모든 컴포넌트(특히 경고 UI, 타이머)가 모바일 환경(iOS/Android)에서 최적화되었는지 확인. | Designer | $\square$ | (현재는 Passed로 가정하나 반드시 크로스체크 필요) |
| **[비즈니스] Funnel 검증** | CTA 문구와 Landing Page의 메시지 일관성(Tone & Voice) 최종 점검. | Business | $\square$ | 'Gap'과 '위기감'을 고조시키는 마지막 문구가 가장 강력한가? |

---
📊 평가: 진행중 — QA 보고서 작성을 완료했지만, 실제 테스트 환경에 대한 접근 권한 및 실시간 로그 모니터링이 필요하여 최종 배포 검증은 불가능합니다.
📝 다음 단계: 개발팀과 협업하여 BUG-001 (타이머 오류)와 BUG-002 (백엔드 유효성 검사 누락)를 수정하고, 수정된 코드를 바탕으로 실제 스테이징 환경에서 E2E 테스트를 재진행해야 합니다.
